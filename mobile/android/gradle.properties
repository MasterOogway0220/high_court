org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin's incremental caches intermittently fail to close on Windows — a file
# lock, usually the on-access virus scanner — and take the build down with
# "Could not close incremental caches". Release builds compile from clean
# anyway, so the caches buy nothing here and cost a flaky build.
kotlin.incremental=false
